#include <stdio.h>
int main()
{
int a,b,diff;
printf("Enter two numbers:");
scanf("%d %d",&a,&b);
if(a>b){
diff = a - b;
printf("Difference = %d",diff);
}
else{
diff = b - a;
printf("Difference = %d",diff);
}
return 0;
}
